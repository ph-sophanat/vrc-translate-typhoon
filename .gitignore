# Rust build output
/target

# Packaged standalone bundle (build artifact)
/dist-bundle

# Python ASR service virtualenv & caches
service/.venv/
__pycache__/
*.pyc

# PyInstaller output from package.ps1 (regenerated by the build)
service/dist/
service/build_pyi/

# Logs
*.log

# Runtime logs and captured audio
translation_log.txt
clips/

# Contains live API keys — never commit. Use config.example.toml instead.
config.toml
